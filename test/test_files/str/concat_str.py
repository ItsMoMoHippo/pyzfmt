# concatenated strings (implicit concatenation)
message = "This is a long string " "that is split across " "multiple parts"

# concatenated strings with different quote styles
text = 'Hello ' "World"

# concatenated with parentheses
long_text = (
    "First line "
    "second line "
    "third line"
)
